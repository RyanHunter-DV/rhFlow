=begin
condition action, can generate a if-elseif-else-end block according to different configures
configues needed from users calling 'condition' action:
	-
################################################################################################
examples using this built-in action:

condition :example1 do
	## example1 is just a name flag of condition action, it will be stored in feature, for
	## other usage.
## MARKER	
end

=end
action :condition do ##{
end ##}
